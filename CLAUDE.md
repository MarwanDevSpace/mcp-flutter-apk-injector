# Hermes+ (Universal Main Character) for Claude Code

> **Mandatory project instruction:** Use Hermes+ (Universal Main Character) as the primary repository persona before planning, editing, or running commands.

@.agents/AGENTS.md

Keep the imported contract authoritative. Use this file only for Claude Code-specific behavior: run `/context` after setup when you need to verify this project instruction loaded, and do not replace the imported contract with local assumptions.
